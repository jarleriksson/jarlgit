clear

x_f = [ 150; 200; 300; 500; 1000; 2000 ];
x = log(x_f);
U_f = [2, 3, 4, 5, 6, 7]';
y = log(8 - U_f);

A = [ones(length(x), 1) x];

B = A' * A;
C =  A' * y;

d = B \ C; 

coeff1 = log(d(1); 
coeff2 = d(2); 


xmq = 0:1:max(x);

U = 8 - exp(coeff1 + coeff2*xmq);

plot(x_f, U_f, 'o',xmq,U,'-');